#include "main.h"
#include<stdio.h>
#include<stdlib.h>
#include<ctype.h>
#include<string.h>

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{                                                 return (1);                                       }                                                 return (0);                                       }
